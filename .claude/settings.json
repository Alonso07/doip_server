{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -q '\\.py$' && black --target-version py311 \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting with Black..."
          }
        ]
      }
    ]
  }
}
